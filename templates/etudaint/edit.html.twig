{% extends 'base.html.twig' %}

{% block title %}Edit Etudaint{% endblock %}

{% block body %}
    <h1>Edit Etudaint</h1>

    {{ include('etudaint/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_etudaint_index') }}">back to list</a>

    {{ include('etudaint/_delete_form.html.twig') }}
{% endblock %}
