<form method="post" action="{{ path('app_etudaint_delete', {'id': etudaint.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etudaint.id) }}">
    <button class="btn">Delete</button>
</form>
